- name: installing mysql
  hosts: mysql
  become: yes
  tasks:
  - name: installing mysql
    ansible.builtin.package:
      name: mysql-server
      state: installed
  - name: starting mysql
    ansible.builtin.service:
      name: mysqld
      state: started
      enabled: yes
  

