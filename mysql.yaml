- Name: installing mysql
  hosts: mysql
  become: yes
  tasks:
  - name: installing mysql
    ansible.builtin.package:
      name: mysql-server
      state: installed
  - name: starting mysql
    ansible.builtin.service:
      name: mysql-server
      state: started
      enabled: yes
  

