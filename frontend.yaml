- name: configuring front end
  hosts: frontend
  become: yes
  tasks:
  - name: installing nginx
    ansible.builtin.package:
      name: nginx
      state: installed
  - name: start and enable nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes